name: Build and Release Electron App

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'

jobs:
  create-release:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Create GitHub Release
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: context.ref.replace('refs/tags/', ''),
              draft: false,
              generate_release_notes: true
            });

  build:
    needs: [create-release]
    if: always()
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Compile TypeScript
        run: npm run build:ts

      # - name: Run tests
      #  run: npm test

      - name: Prepare Windows certificate
        id: cert
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $b64 = "${{ secrets.WINDOWS_CERTS }}" -replace '\s', ''
          if ($b64.Length -gt 0) {
            $bytes = [System.Convert]::FromBase64String($b64)
            [System.IO.File]::WriteAllBytes("$env:RUNNER_TEMP/certificate.p12", $bytes)
            "path=$env:RUNNER_TEMP/certificate.p12" >> $env:GITHUB_OUTPUT
          }

      - name: Build and release Electron app (Windows, no signing)
        if: matrix.os == 'windows-latest' && steps.cert.outputs.path == ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --publish ${{ startsWith(github.ref, 'refs/tags/v') && 'always' || 'never' }}

      - name: Build and release Electron app (Windows, with signing)
        if: matrix.os == 'windows-latest' && steps.cert.outputs.path != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WIN_CSC_LINK: ${{ steps.cert.outputs.path }}
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CERTS_PASSWORD }}
          CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CERTS_PASSWORD }}
        run: npx electron-builder --publish ${{ startsWith(github.ref, 'refs/tags/v') && 'always' || 'never' }}

      - name: Build and release Electron app (Linux)
        if: matrix.os == 'ubuntu-latest'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --publish ${{ startsWith(github.ref, 'refs/tags/v') && 'always' || 'never' }}
